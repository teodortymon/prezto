#
# Executes commands at the start of an interactive session.
#
# Authors:
#   Sorin Ionescu <sorin.ionescu@gmail.com>
#

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

# Customize to your needs...

alias b=batcat
alias m=micro

export NVM_DIR="$HOME/.nvm"
#[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
#[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
alias loadnvm="\. "$NVM_DIR/nvm.sh && \. "$NVM_DIR/bash_completion"
[ -s "/Users/teodor/.scm_breeze/scm_breeze.sh" ] && source "/Users/teodor/.scm_breeze/scm_breeze.sh"

export PATH="$PATH:/opt/yarn-[version]/bin"
git config --global alias.nuke '!sh -c "git branch -D $1 && git push origin :$1" -'
precmd() {
  printf "\e]9;9;%s\e\\" "$(wslpath -m "$PWD")"
}
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/tools
export PATH=$PATH:$ANDROID_HOME/tools/bin
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:'/mnt/c/Users/svrtwo/AppData/Local/Programs/Microsoft VS Code/bin'
# export PATH=$PATH:'/mnt/c/Wdsdsindows/system32/clip.exe'

test -e "${HOME}/.iterm2_shell_integration.zsh" && source "${HOME}/.iterm2_shell_integration.zsh"

[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

[ -s "/home/ted/.scm_breeze/scm_breeze.sh" ] && source "/home/ted/.scm_breeze/scm_breeze.sh"

source /home/ted/.config/broot/launcher/bash/br

export PATH="$HOME/.poetry/bin:$PATH"
